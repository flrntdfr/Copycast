apiVersion: v1
kind: Service
metadata:
  name: copycast
  labels:
    app.kubernetes.io/name: copycast
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: copycast
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8080
      targetPort: http
